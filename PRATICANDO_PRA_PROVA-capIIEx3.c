#include <stdio.h>
#include <conio.h>

float age, mes, wk, day, h, min, seg;

main()
{
	printf("Digite sua idade:\n");
	scanf("%f\n", %age);
	mes = age * 12;
	wk = age *52;
	day = age * 365;
	h = age * (365 * 24);
	min = age * (365 * (24 * 60));
}
